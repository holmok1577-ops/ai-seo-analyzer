# SEO Agent - Анализатор сайтов с AI

FastAPI приложение для автоматического SEO-анализа веб-сайтов с использованием OpenAI GPT. Проект позволяет анализировать сайты и генерировать полноценное SEO-ядро.

## Возможности

- 🔍 **Анализ веб-сайтов** по URL
- 🤖 **Генерация SEO-ядра** с помощью OpenAI GPT
- 📊 **Пошаговый анализ** с логированием процесса
- 🔄 **Асинхронная обработка** задач
- 🌐 **Поддержка прокси** для OpenAI
- 🐳 **Docker-контейнеризация**

## Технологический стек

- **Backend:** FastAPI, Python 3.11
- **AI:** OpenAI GPT-4o-mini
- **Parsing:** BeautifulSoup4
- **HTTP Client:** httpx
- **Containerization:** Docker & docker-compose
- **Frontend:** HTML, Jinja2 Templates

## Быстрый старт

### 1. Настройка переменных окружения

Для удобства настройки в проекте есть файл-шаблон `.env.example`. Скопируйте его в `.env` и заполните своими данными:

```bash
cp .env.example .env
```

Или создайте файл `.env` в корне проекта:

```env
OPENAI_API_KEY=ваш_ключ_или_ключ_прокси
OPENAI_MODEL=gpt-4o-mini
# Если используете прокси, укажите его базовый URL:
# OPENAI_BASE_URL=https://ваш-прокси-домен/v1
APP_HOST=0.0.0.0
APP_PORT=8000
```

**⚠️ Важно:** Файл `.env` содержит секретные данные и добавлен в `.gitignore`. Никогда не загружайте его в репозиторий!

### 2. Запуск с Docker

Самый простой способ запуска - через Docker:

```bash
docker compose up --build
```

### 3. Доступ к приложению

Откройте в браузере: `http://localhost:8000`

## API Эндпоинты

### POST /analyze
Запускает анализ сайта по указанному URL.

**Request:** `application/x-www-form-urlencoded`
- `url` (string) - URL сайта для анализа

**Response:** `application/json`
```json
{
  "task_id": "uuid-строка-идентификатор-задачи"
}
```

### GET /status/{task_id}
Проверяет статус задачи анализа.

**Response:** `application/json`
```json
{
  "done": false,
  "logs": ["Шаг 1: Анализ структуры сайта...", "Шаг 2: Генерация ключевых слов..."],
  "result": null
}
```

При завершении анализа (`done: true`):
```json
{
  "done": true,
  "logs": ["Все шаги выполнены"],
  "result": {
    "keywords": ["ключевое слово 1", "ключевое слово 2"],
    "clusters": [
      {"name": "Название кластера", "items": ["запрос 1", "запрос 2"]}
    ],
    "meta": {
      "description": "SEO описание",
      "h1": "Заголовок H1",
      "title": "Title страницы"
    },
    "notes": "Дополнительные рекомендации"
  }
}
```

## Как это работает

1. **Парсинг сайта:** BeautifulSoup анализирует HTML структуру
2. **Планирование:** GPT создает пошаговый план анализа
3. **Исполнение:** Каждый шаг выполняется с логированием
4. **Синтез:** GPT собирает результаты в финальное SEO-ядро
5. **Возврат:** Результат сохраняется и доступен через API

## Структура проекта

```
seo-agent/
├── app/
│   ├── main.py              # Основное FastAPI приложение
│   ├── static/              # CSS и статические файлы
│   └── templates/           # HTML шаблоны
├── docker-compose.yml       # Docker конфигурация
├── Dockerfile              # Сборка Docker образа
├── requirements.txt        # Python зависимости
├── .env.example            # Шаблон переменных окружения
├── .gitignore              # Исключения для Git
└── README.md               # Этот файл
```

## Требования

- Docker и docker-compose
- API ключ OpenAI (или прокси)
- Интернет соединение

## Настройка прокси

Если у вас нет прямого доступа к OpenAI, можно использовать прокси:

```env
OPENAI_API_KEY=ваш_ключ_прокси
OPENAI_BASE_URL=https://api.proxyapi.ru/openai/v1
```

## Пример использования

1. Откройте `http://localhost:8000`
2. Введите URL сайта (например: `https://example.com`)
3. Нажмите "Анализировать"
4. Скопируйте полученный `task_id`
5. Откройте `http://localhost:8000/status/{task_id}` для отслеживания

## Особенности

- **Асинхронность:** Анализ выполняется в фоновом режиме
- **Логирование:** Каждый шаг подробно логируется
- **Отказоустойчивость:** Обработка ошибок на каждом этапе
- **Масштабируемость:** Docker контейнеризация для легкого развертывания

## Лицензия

MIT License
